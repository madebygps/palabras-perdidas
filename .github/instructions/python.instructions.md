---
applyTo: '**/*.py'
---

- always use `uv run` to run any python script
- always use `uv add` to add any new dependencies
- always use `uv remove` to remove dependencies
- do not use `pip` to manage dependencies
- always write type annotations for functions and methods
- do not write comments unless they add value